package supermarket;

/**
 * DEPRECATED — renamed to {@link CheckoutSession}.
 *
 * The lifetime of this class is one customer's checkout, so the more
 * accurate name is CheckoutSession. The original "CashRegister" name
 * was misleading because a real cash register is the long-lived hardware
 * at the lane, not the per-customer session.
 *
 * This file kept as an empty marker to make the rename visible.
 * Delete it in Eclipse (right-click → Delete) when convenient.
 */
@Deprecated
public class CashRegister {
}
